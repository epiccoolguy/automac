homebrew_path: "{{ (ansible_machine == 'arm64') | ternary('/opt/homebrew', '/usr/local/Homebrew') }}"

homebrew_formulas:
  - alacritty
  - coreutils
  - curl
  - git
  - jq

homebrew_casks:
  - amethyst
  - git-credential-manager
